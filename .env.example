# The static site only reads public, immutable Blob URLs stored in content files.
# Do not expose BLOB_READ_WRITE_TOKEN to browser code.
PUBLIC_MEDIA_BASE_URL=https://your-store-id.public.blob.vercel-storage.com
